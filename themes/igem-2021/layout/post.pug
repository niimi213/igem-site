.contents-wrapper
  if (page.eyecatch)
    div.eyecatch(style={'background-image': `url(${page.eyecatch})`})
  .article-wrapper
    .page-navigation
      != toc(page.content)
    article.post
      != partial('_partial/article', { post: page })
!= js('js/submenu.js')
